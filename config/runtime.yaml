workspace:
  default_root: "./workspace"
  runtime_dir: "_runtime"

logging:
  level: "INFO"
  json: true

human_interface:
  backend: "cli"

agent_behavior:
  max_empty_reply: 2
  max_nudge_finish: 2
  max_validation_retries: 3

debug:
  enable_trace: true

