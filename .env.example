# ResearchOS 环境变量配置示例
# 复制此文件为 .env 并填入实际的配置值
#
# 快速开始：
#   cp .env.example .env
#   nano .env  # 编辑并填入你的 API Key
#
# 注意：
# - .env 文件包含敏感信息，不应提交到版本控制系统
# - .env 文件已在 .gitignore 中排除
# - API key 也可以直接在 config/model_routing.yaml 的 api_keys 部分配置

# ========================================
# LLM API 配置
# ========================================
# 推荐方式：在 config/model_routing.yaml 的 api_keys 部分直接配置
# 环境变量方式（可选）：使用下方变量

# SiliconFlow API（默认使用）
# 获取方式：https://siliconflow.cn
SILICONFLOW_API_KEY=your-siliconflow-key
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# OpenRouter API（可选）
# 获取方式：https://openrouter.ai/keys
OPENROUTER_API_KEY=

# OpenAI API（可选）
# 获取方式：https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic API Key（可选，如果使用 Claude 模型）
# 获取方式：https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ========================================
# 论文搜索 API 配置（可选）
# ========================================

# Semantic Scholar API Key（可选）
# 用于：T2 Scout Agent 的学术论文搜索
# 获取方式：https://www.semanticscholar.org/product/api
S2_API_KEY=

# 研究者邮箱（可选）
# 用于：arXiv API 识别（礼貌性标识）
RESEARCHER_EMAIL=your-email@example.com

# ========================================
# MCP 工具配置（可选）
# ========================================

# GitHub Personal Access Token（可选）
# 用于：GitHub MCP 服务器
# 获取方式：https://github.com/settings/tokens
GITHUB_TOKEN=

# ========================================
# 运行参数说明
# ========================================
# `config/runtime.yaml` 才是运行参数的主配置入口。
# 例如：
# - logging.level
# - debug.enable_trace
# - ui.no_banner
# - web_fetch.allowed_schemes / allowed_hosts
#
# `.env` 里建议只保留：
# - API Key / Token
# - 研究者身份类信息（如 RESEARCHER_EMAIL）
# - 少量必须通过环境变量注入给第三方进程的值
