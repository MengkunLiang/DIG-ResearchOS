# Docker-compatible dependency mirror for `pyproject.toml` runtime + dev extras.
# Keep package constraints aligned with the package metadata; do not add TeX here.

# ResearchOS core runtime
httpx>=0.27,<1
jinja2>=3.1,<4
jsonschema>=4.23,<5
pydantic>=2.7,<3
pyyaml>=6,<7
structlog>=24.1,<26
python-dotenv>=1.0,<2

# LLM routing and provider adapters
litellm>=1.65.8

# Optional external Tool servers configured through config/mcp.yaml.
mcp>=1.13,<2

# Literature, PDF, and bibliography processing
pdfplumber>=0.10,<1
PyMuPDF>=1.23,<2
PyPDF2>=3,<4
bibtexparser>=1.4,<2

# Deterministic academic figures for survey/comparison artifacts
matplotlib>=3.8,<4

# Structured terminal panels, bounded tables, and no-color rendering for research observability.
rich>=13,<14

# Test and development tooling
pytest>=8.2,<9
pytest-asyncio>=0.23,<1
