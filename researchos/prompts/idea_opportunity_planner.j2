Return exactly one JSON object with this shape and no Markdown:
{"opportunities": [OpportunityQuery, ...]}

You are planning the Evidence-Routed Opportunity Map, not writing Candidate Ideas. Use the workspace to ground and calibrate a set of meaningfully different research questions from tensions, hidden assumptions, mechanism gaps, failure boundaries, evaluation blind spots, user-seed challenges, or structurally supported Bridge opportunities.

Return the smallest non-redundant map that gives the enabled Routes useful questions. `opportunity_budget` is a strict output ceiling sized for the eventual researcher decision surface, not a target and not an instruction to create one item per Route. Stop early once additional items would repeat the same causal logic, counterfactual, or validation need.

Keep the object compact enough to finish cleanly. Use one concise paragraph or sentence per prose field, and normally two or three genuinely discriminating `competing_explanations`; do not repeat the same rationale across `one_line_summary`, `question`, `why_it_matters`, and `conceptual_leap`.

For each item, prefer one value from `opportunity_type_values` for `type`; an equivalent concise scientific category is acceptable because this label is descriptive, not a routing or evidence decision. A Route name is not a useful Opportunity category. Put only values from `enabled_route_values` in `compatible_routes`, which is the field that controls routing.

The workspace is a source of context and evidence boundaries, not a closed idea space. Reason over it together with general scholarly knowledge, counterfactual reframing and structural cross-domain analogy. `knowledge_origin` is one internal Opportunity-level provenance summary, not a requirement to label every ordinary inference. Use `mixed` when literature and model reasoning are genuinely fused. Set `verification_required=true` only when an unverified external fact, paper attribution, empirical premise, mechanism attribution or novelty assumption could materially change the Opportunity; ordinary logical connections and design reasoning need no artificial warning label. An Opportunity remains a proposed question, never a verified research gap or external novelty claim.

For each Opportunity, preserve a concise scientific argument when useful: its `conceptual_leap` and one or more `competing_explanations`. These are researcher-reviewable statements of what makes the question non-trivial. Evidence atoms are valuable when present, but their absence must not prohibit a coherent, testable Opportunity derived through scholarly reasoning.

The `workspace_research_context` identifies the subject of this project. Use it to keep Opportunities about that subject. Do not turn ResearchOS, prompt formatting, Evidence Routing, Candidate generation, or the existence of an evidence workflow into the research object unless the project excerpts explicitly study those systems.

Do not turn a missing retrieval area into a factual research gap. Do not invent a source, citation, established finding, available dataset, measured result, novelty claim, score, or final method. Merge wording-only duplicates while preserve genuinely different causal, counterfactual, or validation logic as parallel opportunities.
{{ payload_json }}
