Return exactly one structured JSON object. Do not score, rank, or select Candidates. Keep any explanatory prose inside the requested scientific fields rather than outside the JSON envelope.

You are forming Idea Seeds for one assigned Route, not judging them. Use the workspace-derived Opportunity and Evidence Bundle to ground and calibrate the proposal; they are not a closed boundary on your scientific imagination. You may use general scholarly knowledge, counterfactual reasoning, and structural cross-domain analogy to propose a mechanism, reframing, or validation path that is not explicitly written in the workspace.

Preserve source references, reading levels, uncertainty, and Evidence Permission in every Gene. A full/partial-text source may anchor a bounded claim only within the supplied section; abstract-only, synthesis-inference, and brainstorm material may inspire an opportunity or candidate mechanism but must not be presented as an established mechanism, design rationale, final Claim, or external novelty result. Any non-workspace insight must be visibly conjectural: use `knowledge_origin`, `creative_leap`, `competing_explanations`, `surprising_prediction`, `research_program_potential`, and an explicit verification or reading-upgrade need where useful.

Treat `workspace_research_context` as the project's substantive subject and the supplied evidence atoms as traceable grounding for certified claims, not as the exclusive source of scientific hypotheses. Do not make ResearchOS, prompting, Evidence Routing, Candidate generation, or the evidence workflow itself the proposed research object unless the project or a source excerpt explicitly studies that topic. If the material is too sparse, form a narrow, provisional domain hypothesis and name the exact upgrade needed rather than replacing the project with a meta-workflow proposal.

For a supported Route prefer the minimal initial form:
{"seeds": [IdeaSeed, ...]}

One IdeaSeed needs only a problem, one-line thesis, candidate mechanism, one contribution sketch, one falsifiable prediction, one main uncertainty, and Route origin. A full CandidateDossier is also accepted when the information is already available. Do not force a first-pass Seed to include a complete presentation, multiple hypotheses, a full evidence map, implication analysis, or a final experiment plan. Those fields are enrichable in scoring, mutation, reading upgrades, and final-card compilation. Preserve a concise `creative_leap` when it explains why the Idea is not merely an incremental module stack. Do not copy one Seed into multiple routes with superficial wording changes; distinct causal explanations, counterfactuals, or validation programmes may coexist.

For a Route the current evidence cannot support return:
{"status": "unsupported", "unsupported_reason": "..."}

When `recovery=true` or `candidate_completion.enabled=true`, use the call as a creative re-divergence pass. The requested count is a cost budget, not a requirement to manufacture filler. Return only non-redundant Ideas; it is valid to return fewer or an `unsupported` Route when another proposal would only repeat the existing causal logic. Knowledge-derived details must be marked as proposed: use conjecture/inspiration provenance, set `upgrade_required=true`, state the uncertainty in the Candidate, and make the validation path capable of rejecting it. Never manufacture a paper, citation, dataset, metric, empirical result, or a verified mechanism.

Only `cross_domain_bridge` may add a `bridge_reviews` array. It is optional at initial formation: omit it rather than spending the response budget on a separate governance review when the Route can contribute a stronger Seed or when the transfer needs later reading. If you do provide a Bridge review, make it concrete and evidence-calibrated; use `deferred` or
`no_candidate_available` when appropriate. A missing review is recorded as
`unreviewed` for a later enrichment pass, not treated as a successful bridge decision or as a Route failure. Do not fabricate a Cross-domain Candidate from keyword overlap; require a structural mapping, transfer risk, and evidence-calibrated use. The named Cross-domain directions in `bridge_plan` are user-provided research intent: use their names, why, and queries as creative scaffolding for a conjectural structural transfer even if a dedicated bridge paper has not yet been read. This never certifies the transfer; record the Candidate as conjectural and verification-required. Never return only
`bridge_reviews`: an optional review is not a substitute for an IdeaSeed.
{{ payload_json }}
