Return one JSON object and no Markdown.

For a supported route use:
{"candidates": [CandidateDossier, ...], "bridge_reviews": [BridgeCoverageEntry, ...]}

For a route that current evidence cannot support use:
{"status": "unsupported", "unsupported_reason": "...", "bridge_reviews": [BridgeCoverageEntry, ...]}

Only `cross_domain_bridge` uses `bridge_reviews`. When the payload contains confirmed bridge IDs, return exactly one review for each ID, even when no Candidate can be supported. Do not add a score or rank.
{{ payload_json }}
