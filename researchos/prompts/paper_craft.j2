## 通用写作 Craft（所有 section 适用）

### 段落
- 一段一个核心论点，以 topic sentence 领起。
- 段落间有因果、对比或递进关系，不能只是材料并列堆砌。

### Claim 强度 = Evidence 强度
- `invention`：可用 introduce / first to，但必须有证据链。
- `improvement`：用 improve / extend，禁止 first / novel paradigm。
- `exaptation`：用 adapt / transfer X to Y。
- `routine`：不应作为核心贡献；若出现，在 review/self_check 中标为需要上游 gate 重审，不要写进最终贡献。
- 每个量化 claim 必须指向 result metric 或实验 artifact；没有证据就删除该 claim、弱化表述，或转入 Limitations，不要在最终 TeX 中写 `% TODO(evidence)`。

### 时态 / 语态
- Method 用现在时。
- 实验过程用过去时。
- 普遍事实用现在时。
- 主动语态优先。

### 禁用 AI 套话
不要使用：delve into / it is worth noting / plays a crucial|pivotal|vital role /
in the realm of / rich tapestry / a testament to / shed light on / pave the way /
With the rapid development|advancement of / in today's fast-paced|ever-evolving /
段首连续 3 次以上 Furthermore。

### 引用
- citation key 必须存在于 `paper_state.shared_facts.bib_keys`。
- 引用支撑具体 claim；不要一句话堆 5 个以上引用而不区分。
- 引用前核对该 key 的标题、paper note 或 citation pool 用途；论文主题、方法族、数据/场景和证据类型必须与当前句子的 gap、定位、设计选择、边界或解释 claim 对应。不能用同领域但不相关的文献填充引用数量。
- Abstract 不写正式引用。不要在 Abstract 使用 `\cite{}` / `\citep{}` / `\citet{}` / `\parencite{}` 等命令，也不要写作者-年份或数字引用；具体 prior work citation 放到 Introduction 或 Related Work。

### 数字
- 数字来自 result_metrics 或明确读取的 artifact，保留与来源一致的有效位数。
- 不在多个 section 中用不同小数位重复报告同一结果。

### 对齐关系
- `alignment_matrix.json` 和 `paper_state.json` 负责内部追踪 contribution lane。
- 最终 TeX 正文和注释都不要暴露内部编号，例如 `C1`、`[C1]`、`C1:` 或 `% [C1]`。
- 如果某段兑现某条贡献，用自然语言说明 motivation / design choice / evidence / analysis 的闭环。
