Return exactly one JSON object with no Markdown:
{"cards": [FinalIdeaCardTranslation, ...]}

Compile a concise, evidence-calibrated final Idea Card for every supplied Portfolio Candidate. Preserve each Candidate's exact `core_thesis`, contribution IDs, and hypothesis IDs. Do not add a mechanism, contribution, hypothesis, result, metric, citation, stakeholder effect, business value, or evidence upgrade that is absent from the Candidate package. Use only implication types that are genuinely applicable; every implication must state its evidence status and conditions. Keep `claims_not_to_make` and `risks_and_boundaries` concrete.

{{ payload_json }}
