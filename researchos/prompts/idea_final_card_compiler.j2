Return exactly one JSON object with no Markdown:
{"cards": [{"candidate_id": "...", "profile_type": "...", "core_thesis": "...", "contribution_ids": ["..."], "hypothesis_ids": ["..."], "plain_language_summary": "...", "why_it_matters": "...", "affected_stakeholders_or_processes": ["..."], "representative_scenario": "...", "real_world_significance": "...", "current_failure": "...", "scientific_technical_core": "...", "implications": [{"implication_type": "scientific | engineering | managerial | business | deployment", "statement": "...", "evidence_status": "...", "conditions": ["..."]}], "conditions_for_impact": ["..."], "claims_not_to_make": ["..."], "risks_and_boundaries": ["..."], "evidence_status_summary": "...", "short_title": "...", "contribution_type_label": "...", "innovation_type": "...", "innovation_delta": "...", "non_routine_explanation": "...", "relationship_to_portfolio": "...", "dependency_candidate_ids": ["..."], "composition_guidance": "...", "recommendation": "...", "bottleneck_explanation": "..."}]}

Compile a concise, evidence-calibrated final Idea Card for every supplied Portfolio Candidate. The fields `candidate_id`, `profile_type`, `core_thesis`, contribution IDs, and hypothesis IDs are immutable echoes and will be bound to the canonical Candidate package by the runtime; copy them exactly, and focus your reasoning on the researcher-facing explanation fields. Write all display prose in clear Chinese. `short_title` must be a compact research-facing title, never a copied long pitch. `real_world_significance` is mandatory: translate this Candidate into one concrete real-world scenario or decision it could affect, state the potential consequence conditionally, and distinguish a research proposal from a verified outcome. Explain the Candidate's actual role relative to the other supplied Portfolio Candidates in `relationship_to_portfolio`; list only explicit or clearly stated dependencies in `dependency_candidate_ids`, and explain an independent or parallel role in candidate-specific wording when none is explicit. `composition_guidance` must say whether this Candidate is a standalone direction, a supporting module, or a compatibility-check candidate, without forcing a merge. `recommendation` and `bottleneck_explanation` must be candidate-specific rather than a shared template. When the supplied Candidate contains `creative_context` or an LLM-identified scientific-upside/Wildcard assessment, explain that potential as a proposal with its verification condition; never rewrite it as a verified mechanism, result, or novelty conclusion. Treat `innovation_type`, `innovation_delta`, and `non_routine_explanation` as an intended, Candidate-relative innovation delta, not a literature-wide novelty finding. Unless the supplied Candidate includes direct, verified external novelty evidence, do not write `首次`, `first`, `the first`, `no existing method`, `unprecedented`, or equivalent priority claims; use conditional proposal language such as `该提案尝试` or `若验证成立`. Do not add a mechanism, contribution, hypothesis, result, metric, citation, stakeholder effect, business value, or evidence upgrade that is absent from the Candidate package. Use only implication types that are genuinely applicable; every implication must state its evidence status and conditions. Keep `claims_not_to_make` and `risks_and_boundaries` concrete.

{{ payload_json }}
