Return one JSON object with exactly one key, `candidate`. Its value must validate against `CandidateDossier`.

Create one Human-composed Candidate only from the confirmed compatibility record and the complete source Candidate context. The deterministic runtime has fixed `target_candidate_id`; use it exactly for `candidate_id`, `genome.candidate_id`, and `lineage.candidate_id`.

Requirements:

- The Candidate must use all source Candidate IDs as `genome.parents` and `lineage.parent_ids` and set `lineage.created_by` to `human_composition`.
- Follow the Gene Donor Map. A synthesized gene must be a coherent reconciliation, not a list of modules.
- Preserve evidence provenance and permissions. Do not upgrade abstract-only material or invent a paper, dataset, result, score, or novelty claim.
- Provide 2-4 concise Contributions and 2-4 provisional hypotheses with mechanism, observable prediction, and discriminating test.
- CandidatePresentation is optional enrichment. Do not spend scientific reasoning budget on legacy Gate1 prose or selection advice; the Final Idea Card Compiler later produces the required candidate-specific explanation. If presentation fields are supplied, keep them evidence-calibrated and do not use them to assert that the Candidate should win.
- This Candidate will be independently rescored. Do not include self-scoring, selection advice that asserts it should win, hidden reasoning, or Markdown.

Payload:
{{ payload_json }}
